# =============================================================================
# Glance Docker UFW Application Profile
# =============================================================================
#
# This file defines the UFW application profile for Glance Dashboard (Docker).
# It allows secure access to the Glance web interface from specified networks.
#
# Profile: Glance
# Title: Glance Dashboard (Docker)
# Description: Beautiful dashboard for self-hosted services (Docker)
#
# Author: IanTeda
# =============================================================================

[Glance]
title=Glance Dashboard (Docker)
description=Beautiful dashboard for self-hosted services (Docker)
ports={{ networking_glance.web_port | default('8080') }}/tcp