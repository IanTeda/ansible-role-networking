#-- /etc/nginx/nginx.conf

# Nginx Main Configuration
# =======================
#
# This template provides a secure and optimised Nginx configuration with:
#   - Automatic worker process scaling
#   - Modern TLS/SSL security settings (TLSv1.2+)
#   - Performance optimisations (sendfile, tcp_nopush, gzip)
#   - Security hardening (server tokens off, security headers)
#   - Standard Debian/Ubuntu directory structure
#
# Performance Settings:
#   - worker_processes: auto (scales with CPU cores)
#   - worker_connections: 768 (adjust based on server capacity)
#   - Gzip compression: enabled with level 6
#
# Security Features:
#   - TLSv1.2+ only (no older protocols)
#   - Strong cipher preferences
#   - Security headers (X-Frame-Options, X-Content-Type-Options, X-XSS-Protection)
#   - Server tokens disabled (hides Nginx version)
#
# File Structure:
#   - Site configs: {{ nginx_sites_enabled }}
#   - Site location configs (/<location>): {{ nginx_sites_location }}
#
# Note: Customise worker_connections and buffer sizes based on your server resources.

user www-data;
worker_processes auto;
pid /run/nginx.pid;
include /etc/nginx/modules-enabled/*.conf;

events {
    worker_connections 768;
    # multi_accept on;
}

http {
    # Basic Settings
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    server_tokens off;  # Don't show nginx version

    # Mime Types
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Gzip Settings
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_buffers 16 8k;
    gzip_http_version 1.1;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Security headers
    add_header X-Frame-Options SAMEORIGIN;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    
    # Virtual Host Configs
    include {{ nginx_sites_enabled }}/*;
}